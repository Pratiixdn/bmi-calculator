services:
  - type: web
    name: bmi-calculator
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py migrate
    startCommand: gunicorn bmi_project.wsgi --bind 0.0.0.0:$PORT